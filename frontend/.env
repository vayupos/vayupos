# Default environment variables (used as fallback)
# In production (Vercel), these will be used if VITE_ env vars aren't set via dashboard
VITE_API_URL=http://127.0.0.1:8000/api/v1
VITE_API_HOST=http://127.0.0.1:8000
